/**
 * Re-export do Logo de branding para manter compatibilidade com imports existentes.
 * Dashboard, Login, Register, Landing, NotFound continuam usando @/components/common.
 */
export { default, Tagline } from '../brand/Logo'
